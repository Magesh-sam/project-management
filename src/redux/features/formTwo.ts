import { createSlice } from "@reduxjs/toolkit";

export const formTwoSlice = createSlice({
  name: "formTwo",
  initialState: {
    formTwo: {},
  },
  reducers: {},
});

export default formTwoSlice.reducer;